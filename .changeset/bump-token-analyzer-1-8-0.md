---
"@recursica/adapter-mui-v7": patch
---

Bumped `@recursica/token-analyzer` to 1.8.0, which recognizes the `_modes_` naming used by newer Forge token exports (previously only `_themes_`) so legitimate mode/layer backing variables stop being flagged as unused.
