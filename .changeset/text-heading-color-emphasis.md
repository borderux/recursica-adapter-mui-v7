---
"@recursica/adapter-mui-v7": minor
---

Text and Heading now support `color`/`emphasis` props; fixed FormControlLayout squeezing the whole label+control row (instead of just the control) when `controlMaxWidth`/`controlMinWidth` is set. Also synced the Forge token export to 0.28.2 and bumped `@recursica/adapter-tester` to `^5.2.0`.
