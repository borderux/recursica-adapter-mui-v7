---
"@recursica/adapter-mui-v7": patch
---

Fixed broken relative links in CONTRIBUTING.md's `adapter-tester` README/AGENT references that pointed back into the monorepo and no longer resolved after this repo's split into a standalone repo. They now use absolute GitHub URLs.
